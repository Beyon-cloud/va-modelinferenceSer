apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: virtual-assistant-ingress
spec:
  rules:
  - host: virtual-assistant.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: virtual-assistant-service
            port:
              number: 80
